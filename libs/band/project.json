{
  "root": "libs/band",
  "sourceRoot": "libs/band/src",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/band",
        "main": "libs/band/src/index.ts",
        "tsConfig": "libs/band/tsconfig.lib.json",
        "assets": ["libs/band/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/band/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/band"],
      "options": {
        "jestConfig": "libs/band/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
